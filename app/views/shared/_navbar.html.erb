<section class="navbar">
    <div class="nav-container">
        <div class="brand">
            <a href="#!"><%= image_tag("odin_brand.svg", class: "logo") %>Odinbook</a>
        </div>
        <nav>
            <ul class="nav-list">
                <% if user_signed_in? %>
                    <li><a href="<%= posts_path %>">Posts</a></li>
                    <li><a href="<%= destroy_user_session_path %>">Sign-out</a></li>
                <% else %>
                    <li><a href="<%= new_user_session_path %>">Sign-in</a></li>
                    <li><a href="<%= new_user_registration_path %>">Register</a></li>
                <% end %>
            </ul>
        </nav>
    </div>
</section>